'use client';

import { useEffect } from 'react';
import { useRouter } from 'next/navigation';

export default function DashboardPage() {
  const router = useRouter();

  useEffect(() => {
    // Redirect to the patients tab
    router.replace('/dashboard/patients');
  }, [router]);

  return null;
}
